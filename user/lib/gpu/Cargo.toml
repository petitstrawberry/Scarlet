[package]
name = "gpu"
version = "0.16.0"
edition = "2024"

[dependencies]
scarlet-std = { path = "../std" }
framebuffer = { path = "../framebuffer" }
gpu-raw = { path = "../gpu-raw" }

[lib]
name = "gpu"
path = "src/lib.rs"
