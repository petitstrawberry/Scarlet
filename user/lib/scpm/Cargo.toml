[package]
name = "scpm"
version = "0.1.0"
edition = "2024"

[dependencies]
scarlet-std = { path = "../std", features = ["alloc_debug"] }
miniz_oxide = { version = "0.7", default-features = false, features = ["with-alloc"] }
tar-no-std = "0.4"
