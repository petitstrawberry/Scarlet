[package]
name = "scarlet_std"
version = "0.15.0"
edition = "2024"

[lib]
name = "scarlet_std"
path = "src/lib.rs"

[features]
# Enables allocator overlap/invariant checks even in release builds.
alloc_debug = []

[build]
target = "../../../targets/riscv64-unknown-scarlet.json" 