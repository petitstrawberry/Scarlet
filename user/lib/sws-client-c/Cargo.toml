[package]
name = "sws-client-c"
version = "1.0.0"
edition = "2024"

[lib]
name = "sws_client_c"
crate-type = ["cdylib"]

[dependencies]
sws-client = { path = "../sws-client", default-features = false, features = ["std"] }
scarlet-sys = { path = "../scarlet-sys" }
