[package]
name = "sas-protocol"
version = "1.0.0"
edition = "2024"

[features]
default = ["std"]
legacy-scarlet-std = ["dep:scarlet-std"]
std = []

[dependencies]
scarlet-std = { path = "../std", optional = true }

[lib]
name = "sas_protocol"
path = "src/lib.rs"
