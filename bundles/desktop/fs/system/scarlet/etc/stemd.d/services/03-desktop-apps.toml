# Desktop applications that provide a resident SBUS endpoint.
# They start after SWS, but keep their normal windows hidden until requested.

[service.desktop-files]
exec = "/bin/files"
order = 03
depends = ["sws"]

[service.desktop-launcher]
exec = "/bin/launcher"
order = 03
depends = ["sws"]
